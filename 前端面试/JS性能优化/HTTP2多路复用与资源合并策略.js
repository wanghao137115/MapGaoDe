```

这是一个非常经典的前端性能优化话题。在HTTP/1.1时代，资源合并（如雪碧图、JS/CSS打包）是提高性能的常用手段；而HTTP/2的多路复用特性，则让这些传统优化策略变得不再必要，甚至可能适得其反。

下面我们来深入对比这两种思路，并探讨在HTTP/2时代该如何调整优化策略。

1. 核心原理对比
传统策略：资源合并 (HTTP/1.1)
在HTTP/1.1中，浏览器对同一个域名有并发连接数限制（通常是6-8个）。如果页面有100个资源，就会发生队头阻塞：请求需要排队，只有前几个下载完，后面的才能开始。
为了解决这个问题，开发者会把所有的小图片合并成一张雪碧图，把所有的JavaScript合并成一个bundle.js。这样，100个小请求就变成了1个大请求，以此避开并发限制。

HTTP/2 多路复用
HTTP/2引入的二进制分帧层彻底改变了这一格局。
它允许客户端和服务器在一个TCP连接中，同时发送多个请求和响应，而这些请求/响应之间互不干扰。

原理：它将传输的信息分割为更小的帧，并对它们打上标记。几个请求的帧可以混合在一起发送，到达目的地后再根据标记重新组合。

效果：多个请求并行传输，不会阻塞。浏览器可以同时请求CSS、JS和图片，带宽利用率更高。

2. 为什么HTTP/2下资源合并可能适得其反？
虽然多路复用很强大，但如果继续沿用HTTP/1.1的资源合并策略，可能会带来一些负面影响：

缓存利用率降低

合并前：修改一行JS代码，浏览器只需重新下载那个1kb的小文件，其他9kb的JS和100kb的CSS都能继续使用缓存。

合并后：修改一行代码，会导致整个1mb的bundle.js缓存失效，用户需要重新下载完整的打包文件，浪费流量和带宽。

加载优先级模糊
多路复用允许浏览器发出请求优先级信号（如priority）。如果文件合并了，浏览器就无法优先加载关键的首页CSS，而必须等待整个大文件下载完成，这会拖慢首屏渲染。

解析与执行开销
合并后的巨大文件在解析和执行阶段也会占用主线程，阻塞页面渲染。而HTTP/2允许并发传输，浏览器可以一边下载小文件，一边解析和执行已到达的文件。

3. 最佳实践：中庸之道
在HTTP/2时代，推荐采用中庸策略，既不完全合并为一个文件，也不拆分成成千上万个极小的文件。

建议规则：
将业务逻辑拆分成粒度适中的模块（如每个几十KB），利用HTTP/2多路复用来并行加载。

CSS/JS：

基础库：react.js、vue.js、lodash.js等第三方库变动极低，单独打包成一个vendor.js，利用强缓存长期保留。

业务组件：按页面或功能打包成chunk（如home.js，about.js），或者按路由拆分。

公共模块：使用Webpack/Vite的SplitChunksPlugin，提取多页面间共享的模块。

图片：

小图标：停止使用雪碧图。HTTP/2可以并发加载多个小图。推荐使用内联<svg>或Web Font。

大图：懒加载即可，无需合并。

域名分片：

规则：在HTTP/1.1时代，将资源放到多个域名下（如1.static.com，2.static.com）可以突破连接数限制。

HTTP/2：由于多路复用，通常建议关闭域名分片，因为建立多个TCP连接反而会消耗更多资源（TCP握手、TLS握手），且无法共享同一个拥塞控制策略。

4. 实践中的权衡
Server Push (服务器推送)：HTTP/2允许服务器在客户端请求HTML时，主动将所需的CSS/JS推送给客户端。但使用需谨慎，因为可能推送浏览器缓存中已有的资源，反而浪费带宽。目前正逐渐被103 Early Hints状态码替代。

带宽竞争：虽然多路复用解决了请求层面的阻塞，但多个资源仍然共享同一TCP连接的带宽。如果同时请求很多大文件，可能会产生带宽竞争。这也是需要合理拆分、避免极细小碎文件的原因。

5. 总结
特性	HTTP/1.1 优化策略	HTTP/2 推荐策略
请求数量	极力减少 (合并)	适中数量 (几百个都没问题)
文件粒度	单个大文件	多个独立小/中文件
缓存粒度	粗，一行代码改全部缓存失效	细，只更新修改的那个模块
加载方式	串行排队	并发传输
图片处理	雪碧图	单个图标 (Icon)，独立加载

```