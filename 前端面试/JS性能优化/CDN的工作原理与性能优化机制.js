```
CDN（Content Delivery Network，内容分发网络）是提升网页加载速度的核心基础设施。它就像一个分布在全球的“快递仓储系统”，能把网站的静态资源提前存放到离用户最近的地方。

下面从几个关键维度来拆解CDN提升速度的原理。

1.就近访问 2.减轻源站压力 3.优化资源调度 4.提升可用性

1. 地理就近原则：缩短物理距离
这是CDN最基础的加速原理。光在光纤中的传输速度虽然快，但物理距离带来的延迟是客观存在的。

传统模式：用户请求必须穿越千山万水，到达源站服务器。如果用户在上海，源站在美国，请求就得横跨太平洋。

CDN模式：CDN在全球部署了大量边缘节点。当用户访问时，DNS解析会将用户导向地理上最近的节点。上海的用户请求会被解析到上海的CDN节点，数据无需再跑到美国。

2. 减少“最后一公里”网络拥塞
互联网的骨干网通常速度很快，真正的瓶颈往往在“最后一公里”，即从城市骨干网到用户家庭路由器的这一段。

优化逻辑：CDN节点通常部署在运营商的骨干节点机房附近，相当于直接接入高速公路入口。当用户从CDN节点下载数据时，数据从骨干节点到用户端的距离被大幅缩短，受小区网络高峰拥堵影响的概率也大大降低。

3. 绕过网络运营商之间的互联瓶颈
在中国，电信、联通、移动之间的互联带宽是有限的，跨网访问常常很慢。

优化逻辑：大型CDN服务商会在这三大运营商内部都部署节点。当电信用户访问一个部署在联通机房的网站时，请求通过CDN调度，会由CDN在电信侧的节点返回数据，从而巧妙地避开了跨网慢的问题。

4. 负载均衡：为源站减负
源站服务器的处理能力和带宽是有限的。如果所有用户都直接请求源站，服务器在高并发下很容易过载。

优化逻辑：CDN承担了绝大部分的流量。对于图片、CSS、JS等静态资源，直接从CDN节点返回，源站服务器只需要处理动态请求或首次回源。这能让源站的带宽消耗降低80%以上，同时使网站能承载更多并发用户。

5. 连接复用与TCP优化
CDN节点和源站之间会建立长连接。假设有1万个用户请求同一个文件，传统模式下源站需要建立1万个短连接；而CDN模式下，节点只需和源站建立一个连接获取文件，后续用户都从节点缓存获取。这就节省了大量TCP握手的时间。

此外，CDN还会在节点层面优化TCP协议栈，比如采用更激进的TCP拥塞控制算法，这在弱网环境下能显著提升传输速度。

6. 缓存与预热
缓存：当一个用户访问了某个资源，CDN节点就会把它存下来。下一个用户访问同样的资源时，直接从内存中读取返回，速度极快。

预热：在活动开始前，网站运营人员可以主动将资源推送到所有CDN节点。这样，活动开始瞬间，所有用户都在本地节点读取，能有效避免因缓存未命中导致的瞬时回源压力。

7. 协议优化
CDN节点还会对传输协议进行现代化改造：

HTTP/2 和 HTTP/3：CDN可以自动将源站的传统HTTP/1.1协议转换成HTTP/2或HTTP/3，利用多路复用、头部压缩、0-RTT等特性减少延迟。

TLS/SSL 握手优化：HTTPS的握手过程非常耗时。CDN节点可以代为处理这些计算密集型工作，并通过会话复用技术，让用户连接节点时快速完成加密协商。

8. 其他加速技术
边缘计算：在一些高级CDN中，可以在节点上直接运行轻量级代码处理请求，无需回源。

智能压缩：CDN可以根据请求头中的 Accept-Encoding，自动将静态资源压缩成gzip或brotli格式，进一步减小传输体积。
```